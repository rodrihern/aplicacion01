import './ItemListContainer.css'

export const ItemListContainer = ( { greeting } ) => {
    return (
        <div id="item-container">
            <h2>{ greeting }</h2>
            
        </div>
    );
}